// numbers

// let userId: number;
// userId = 56 + 5 + 44; //not recomended

let userId = 46446; // whecn immediately value assigned ts automatically checks the type saftey and no need to specify its type
userId.toFixed();

// booleans

let loggedIn: boolean = true;
console.log(loggedIn.valueOf());
export {};
