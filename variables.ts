let greetings: string = "Hello Aryan";
let myNum = 6;

greetings.toUpperCase();

console.log(greetings.toUpperCase());

export {}; // This tells the compiler that this file is a module and not a script. This is done to avoid variable name conflicts.
